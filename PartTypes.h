#pragma once

enum class PartType {
    Head, Body, Arm, Leg, Other
};

enum class ViewType {
    Front, Back, Left, Right, Top, Bottom
};

enum class ExportScaleUnit {
    UnityMeters,
    MayaCentimeters
};
